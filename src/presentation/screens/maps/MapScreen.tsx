import { StyleSheet, View } from "react-native";
import { Map } from "../../components/maps/Map";

export const MapScreen = () => {
  return (
    <View style={{ flex: 1 }}>
      <Map />
    </View>
  );
};
